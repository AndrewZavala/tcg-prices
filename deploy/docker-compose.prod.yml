# Production overlay — use with base compose:
#   docker compose -f docker-compose.yml -f deploy/docker-compose.prod.yml up -d

services:
  postgres:
    ports: []
    restart: unless-stopped

  web:
    ports: []
    restart: unless-stopped
    environment:
      CORS_ORIGINS: ${CORS_ORIGINS:-https://tcgprices.aizo.solutions}

  scheduler:
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      SITE_DOMAIN: ${SITE_DOMAIN:-tcgprices.aizo.solutions}
      ACME_EMAIL: ${ACME_EMAIL:-}
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      web:
        condition: service_started

volumes:
  caddy_data:
  caddy_config:
